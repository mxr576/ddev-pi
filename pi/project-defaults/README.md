#ddev-generated
# Project defaults for Pi

This directory is versioned with the parent project.

- Put shared Pi settings in `settings.json`.
- Add provider registration files in `providers/` only when your project actually needs them.
- Add model registration files in `models/` only when your project actually needs them.
- Use `extensions/`, `skills/`, `prompts/`, and `themes/` for project-specific Pi resources.
